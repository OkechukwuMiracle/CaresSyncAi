{
  "name": "caresync-ai",
  "version": "1.0.0",
  "description": "AI-powered Patient Follow-Up Reminder & Insight Assistant for healthcare professionals",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["healthcare", "ai", "saas", "patient-management"],
  "author": "CareSync AI",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
